<h1>Teams</h1>
<% @teams.order_teams.each do |team| %>
  <h3><%= team.name %>  <%= link_to "Update #{team.name}", "/teams/#{team.id}/edit" %></h3>
  <p>Created At: <%= ' ' + team.created_at.to_s %></p>
  <% end %> 
  <%= link_to "New Team", "/teams/new" %>  